# יומן פרויקט — מדוקס

תיעוד השינויים והעבודה שנעשו במערכת. הפרויקט כולל שתי מערכות במאגר אחד:
- **קרן** — האפליקציה המקורית, בשורש המאגר.
- **אינה (`inna-gat/`)** — עותק עצמאי עם ניהול בני משפחה.

---

## אינה גת — מערכת חדשה עם ניהול משפחה

**עותק עצמאי של המערכת עבור אינה** (תיקייה `inna-gat/`), נפרד לגמרי מקרן — בסיס נתונים, אחסון וקוד כניסה משלו.

- **ניהול בני משפחה (דינמי, בלי הגבלה):** מסך הבית מציג כרטיסי בני משפחה. אינה מקימה בעצמה כל תיק שתרצה (שם, קרבה, תאריך לידה, צבע), עורכת ומוחקת.
- **תיק אישי לכל בן משפחה:** לחיצה על בן משפחה פותחת את התיק שלו — תיקיות לפי תחום רפואי, חיפוש, העלאה, שליחה — מסונן לאותו אדם בלבד.
- **כפתור "שתף":** שליחת מסמכים דרך תפריט השיתוף המובנה של הטלפון (Gmail/וואטסאפ וכו') — בלי תלות בשירות מייל חיצוני. שליחת המייל (Resend) נשארה כגיבוי אופציונלי.
- **איור משפחה בסגנון קווי אחיד** במקום אימוג'י, להתאמה לשאר האייקונים.
- **פריסה:** פרויקט Vercel נפרד (Root Directory = `inna-gat`), בסיס נתונים Neon נפרד, אחסון Blob נפרד, וקוד כניסה נפרד. כתובת: `meddocs-inna-gat.vercel.app`. מדריך מלא ב-`inna-gat/DEPLOY.md`.

---

## תיקונים ושיפורים (שתי המערכות)

- **שמירת סימון בין קטגוריות:** בעבר, סימון מסמכים למייל התאפס במעבר בין תיקיות. עכשיו הסימון נשמר בין הקטגוריות — אפשר לאסוף קבצים מכמה תיקיות ולשלוח/לשתף אותם יחד.

---

## כלי תחזוקה ושחזור (מערכת קרן) — בעמוד `/admin/recover`

נבנו לאחר שהתגלה שחלק מהעלאה גדולה "נפלה" (קבצים שנשמרו באחסון אך לא הופיעו באפליקציה):

- **בדיקת קבצים שנפלו:** משווה בין הקבצים באחסון לבין המסמכים באפליקציה ומזהה "יתומים" (קבצים לשחזור). *(נמצאו 3 קבצים שנפלו.)*
- **שחזור:** מחזיר את הקבצים היתומים לאפליקציה בלחיצה, כולל ניסיון לחלץ מטא-דאטה.
- **ספירת עמודים:** סופר את סך כל העמודים בכל המסמכים (לא רק מסמכים).
- **בדיקת כפילויות:** מזהה עותקים זהים לחלוטין (לפי טביעת אצבע SHA-256) ושמות דומים, עם קישור לצפייה ואפשרות למחוק עותק מיותר. *(נמצא זוג כפילות אחד, נוקה.)*

---

## פתוח להמשך

- **כתובת קבועה לקרן:** להסדיר את הדומיין (`meddocs.vercel.app` / `kerenb.cura-clinic.co.il`) כך שיצביע תמיד לגרסה העדכנית, בלי צורך להעתיק כתובת deployment בכל פעם.
- **אינה — אפליקציה בחנויות:** רעיון להפוך את המערכת לאפליקציה ב-App Store / Google Play (נדונו: עטיפה, מודל תשלום, חשבונות משתמשים, פרטיות).
- **אינה — איור אישי לכל בן משפחה:** רעיון לעתיד (בהמתנה).
